import React from "react";

const footer = (props) => (
    <footer className="bg-dark text-white text-center p-2">
        @Christophe CATHERINE - Tout droit réservé
    </footer>
);

export default footer;